<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Android 13+ requires this to show any notification at all. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Lets a scheduled reminder survive the phone being switched off and on. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Needed for the reminder to fire at the exact time picked, not "roughly
         around" that time — important for a medicine reminder specifically.
         This one requires the user to grant it via a Settings flow
         (requestExactAlarmsPermission, called in NotificationService). -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!-- A newer, separate exact-alarm permission that's auto-granted at
         install time (for apps not going through Play Store review) rather
         than needing the user to flip a Settings toggle — kept alongside
         SCHEDULE_EXACT_ALARM above as a more reliable backup path to the
         same capability, not a replacement for it. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <!-- Needed for the reminder notification's vibration pattern. -->
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- Needed for fullScreenIntent notifications (wake the screen, show
         over the lock screen) — the same delivery path alarm-clock apps
         get, used for medicine reminders here for the same reason. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <!-- Needed for the Pattern & Voice game's speech recognition. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:label="smriti"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- flutter_local_notifications does NOT declare these itself (its
             own bundled manifest only has 2 permission lines) — without
             them, AlarmManager has nowhere to actually deliver a scheduled
             alarm's broadcast to, so it fires and gets silently dropped.
             This was the real cause of reminders never posting: every
             other fix tried (battery whitelist, exact-alarm permission,
             Autostart) was irrelevant next to a broadcast with no receiver
             to catch it. Copied from the plugin's own example app. -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Lets Android find the on-device speech recognizer for speech_to_text. -->
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
    </queries>
</manifest>
